import { path, isAuthorized } from '../utils'
import clientController from '../controllers/clientController'

export default app => {
  // app.post(path('/client'), isAuthorized, clientController.create)
  // app.get(path('/client/:id'), isAuthorized, clientController.get)
  // app.get(path('/client'), isAuthorized, clientController.find)
  // app.patch(path('/client/'), isAuthorized, clientController.patch)
  // app.del(path('/client/'), isAuthorized, clientController.delete)
}
