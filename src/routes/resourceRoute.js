import { path, isAuthorized } from '../utils'
import resourceController from '../controllers/resourceController'

export default app => {
  // app.post(path('/resource'), isAuthorized, resourceController.create)
  // app.get(path('/resource/:id'), isAuthorized, resourceController.get)
  // app.get(path('/resource'), isAuthorized, resourceController.find)
  // app.patch(path('/resource/'), isAuthorized, resourceController.patch)
  // app.del(path('/resource/'), isAuthorized, resourceController.delete)
}
