import { path, isAuthorized } from '../utils'
import bookingController from '../controllers/bookingController'

export default app => {
  // app.post(path('/booking'), isAuthorized, bookingController.create)
  // app.get(path('/booking/:id'), isAuthorized, bookingController.get)
  // app.get(path('/booking'), isAuthorized, bookingController.find)
  // app.patch(path('/booking/'), isAuthorized, bookingController.patch)
  // app.del(path('/booking/'), isAuthorized, bookingController.delete)
}
